const conf ={
    appwriteUrl: String(import.meta.env.VITE_APPWRITE_URL),
    appwriteUrl: String(import.meta.env.VITE_PROJECT_ID),
    appwriteUrl: String(import.meta.env.VITE_DATABASE_ID),
    appwriteUrl: String(import.meta.env.VITE_COLLECTION_ID),
    appwriteUrl: String(import.meta.env.VITE_BUCKET_ID),
}

export default conf